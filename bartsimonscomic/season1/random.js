const randlinks1 = [
    "./harold",
    "./haroldsequel",
    "./argument",
    "./playship"
];
function grandlinks1() {
    const ra = Math.floor(Math.random() * links.length);
    return links[ra];
}
